<!DOCTYPE html>
<html lang="en">
   <head>
      <meta charset="UTF-8" />
      <meta http-equiv="X-UA-Compatible" content="IE=edge" />
      <meta name="viewport" content="width=device-width, initial-scale=1.0" />
     
      <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6"
      crossorigin="anonymous"
   />

      <title>DashBoard</title>
   </head>
   <body>
      <nav class="navbar">
         <img src="/logo.png" class="logo" width="50" height="50" /></a>
      </nav>
      <main>
         <%if(typeof user == "undefined"){  %>
            <h1>Welcome <%=uname%> </h1>
            
         <% } else {%>
            <h1>Welcome <%=user%> </h1>
         <form action="/users/survey" method="GET">
             <button type="submit" value="<%=user%>" name="getUser">Survey</button>
         </form>
         <% } %>
      </main>
      <h3>Your Games</h3>
      <% if(typeof userRecommendations != "undefined"){ %>
         <ul class="list-group">
         <% var rec = userRecommendations; 
         var recArr = rec.split(',');
         console.log(recArr);
         recArr.forEach((g) => {%>
            <li class="list-group-item"><%=g%></li>
        <% });%>
      </ul>
      <%}%>
   </body>
</html>



  
